'use strict';

/**
 * cookie configs
 */
export default {
  domain: '', // cookie domain
  path: '/', // cookie path
  httponly: false, //是否 httponly
  secure: false, //是否在 https 下使用
  timeout: 86400 //cookie 有效时间
};